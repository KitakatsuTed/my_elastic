<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
        <% artist.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :birth %>
    <%= form.date_select :birth, { start_year: 1950, end_year: Time.zone.now.year } %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.select :gender, Artist.genders_i18n.invert %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
